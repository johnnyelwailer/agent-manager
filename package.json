{
  "name": "agent-manager",
  "version": "0.1.0",
  "type": "module",
  "description": "Local agent orchestration engine — spawns, observes, and manages AI agent sessions via CLI adapters",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test --experimental-strip-types src/**/*.test.ts",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
