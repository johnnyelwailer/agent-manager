{
  "name": "agent-manager",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '@agent-manager/server' dev",
    "test": "bun run --filter '*' test",
    "check": "bun run --filter '*' check"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
