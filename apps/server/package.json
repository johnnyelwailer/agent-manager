{
  "name": "@agent-manager/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test",
    "check": "bun x tsc --noEmit"
  },
  "dependencies": {
    "@agent-manager/shared": "workspace:*",
    "@github/copilot-sdk": "^0.1.0",
    "hono": "^4.7.0",
    "@hono/zod-validator": "^0.4.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
